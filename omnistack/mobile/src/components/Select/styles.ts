import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  selectView: {
    backgroundColor: '#FFF',
    borderRadius: 8,
    marginTop: 4,
    marginBottom: 16,
  },

  select: {
    color: '#C1BCCC',
  },
});

export default styles;
